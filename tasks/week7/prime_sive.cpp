#include <iostream>
#include <vector>

using namespace std;


vector<bool> primes_by_sive(int N){
    vector<bool> is_primes(N+1);
    for (i = 0; i<is_primes.size(), )

}

int main(){
    return 0;
}